[project]
name = "brca-multiomics-validation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Daacoda", email = "111888923+Daacoda@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "snakemake>=9.26.1",
    "tqdm>=4.70.0",
]

[project.scripts]
brca-multiomics-validation = "brca_multiomics_validation:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
